# Shared tooling config for the series. Each sample keeps its own
# requirements.txt so the samples stay independent.

[tool.ruff]
line-length = 100

[tool.ruff.lint]
extend-select = ["I"]   # keep imports sorted
